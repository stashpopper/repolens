Structured output parsing failed: Function DocsOutput arguments: {"project_overview": "# Project Overview\n\n## What the Project Does\nThis is a full-stack web application that provides mood-based music recommendations and lyrics search functionality. It consists of a React frontend and a Node.js/Express backend with PostgreSQL database integration. The application allows users to:\n- Authenticate via login/registration\n- Receive music recommendations based on mood or AI-generated suggestions\n- Search for song lyrics\n- View community-shared songs\n- Access their search history\n\n## Tech Stack\n- **Frontend**: React (with Vite as the build system)\n- **Backend**: Node.js with Express\n- **Database**: PostgreSQL (managed via Prisma ORM)\n- **Authentication**: JWT-based authentication\n- **External APIs**: Last.fm, Lyrics.ovh, Genius, YouTube, and Mistral AI for recommendations\n- **Deployment**: Frontend hosted on Netlify, backend on Vercel\n\n## Architecture\nThe project follows a frontend-backend separation pattern with REST API-based communication. The frontend is a React application served via Vite, while the backend is a Node.js/Express server. Data flows between the frontend and backend through API calls, with the backend interacting with PostgreSQL for data persistence and external APIs for additional functionality like lyrics and recommendations.\n\n## Entry Points\n1. `package.json`: Main project configuration with scripts for starting the backend, building the frontend, and running development servers.\n2. `backend/server.js`: Main backend entry point.\n3. `frontend/src/main.jsx`: React application entry point.\n\n## Build System\nThe project uses Vite for frontend development and builds, inferred from the `npm run dev` script and typical React/Vite project structures.", "file_breakdown": "# File Breakdown\n\n## Frontend Components\n### `frontend/src/components/Auth.jsx`\n- **Purpose**: Manages the authentication UI, toggling between login and registration forms, and redirecting authenticated users to the recommendations page.\n- **Key Functions**:\n - `toggleForm`: Switches between login and registration views.\n - `useEffect`: Redirects to recommendations when the user is authenticated.\n- **Connections**: Uses `Login` and `Register` components, interacts with `setActiveTab` and `isAuthenticated` props to manage UI state.\n\n### `frontend/src/components/Login.jsx`\n- **Purpose**: Handles user login form submission with validation and error handling.\n- **Key Functions**:\n - `handleChange`: Updates form state on input changes.\n - `handleSubmit`: Validates inputs and calls the `login` function with credentials.\n- **Connections**: Used by `Auth.jsx`, calls `login` prop function for authentication.\n\n### `frontend/src/components/Register.jsx`\n- **Purpose**: Handles user registration form submission with validation and error handling.\n- **Key Functions**:\n - `handleChange`: Updates form state on input changes.\n - `handleSubmit`: Validates inputs and calls the `register` function with credentials.\n- **Connections**: Used by `Auth.jsx`, calls `register` prop function for authentication.\n\n### `frontend/src/components/LastFmImage.jsx`\n- **Purpose**: Fetches and displays album/artist images from the Last.fm API.\n- **Key Functions**:\n - Fetches images using artist and track names.\n - Falls back to a default image if the fetch fails.\n- **Connections**: Used by components like `Community.jsx` and `Recommendations.jsx` to display images.\n\n## Frontend Context and Configuration\n### `frontend/src/context/AuthContext.jsx`\n- **Purpose**: Provides authentication context for the React application, managing user state, authentication status, and login/register/logout functionality.\n- **Key Functions**:\n - `useAuth`: Custom hook to access the authentication context.\n - `AuthProvider`: Component that wraps the application to provide auth state.\n - `register`: Handles user registration with the backend API.\n - `login`: Handles user login with the backend API.\n - `logout`: Clears user authentication state.\n - `checkAuthStatus`: Verifies authentication status on app load.\n- **Connections**: Used by other React components to access auth state via `useAuth` hook; connects to backend API endpoints (`/api/auth/register`, `/api/auth/login`, `/api/auth/user`).\n\n### `frontend/.env.development` and `frontend/.env.production`\n- **Purpose**: Define environment variables for development and production, specifically the API URL pointing to the backend server.\n- **Connections**: Used by the frontend application to configure API endpoints during development and production.\n\n### `frontend/vite.config.js`\n- **Purpose**: Configures Vite build tool for the React frontend, enabling JSX support.\n- **Key Functions**: `defineConfig` — Vite configuration function.\n- **Connections**: Used during frontend builds and development.\n\n## Backend Routes and Services\n### `backend/src/routes/lyricsRoutes.js`\n- **Purpose**: Defines the API route for fetching song lyrics by title.\n- **Key Functions**:\n - `getLyrics`: Handles GET requests to fetch lyrics for a song.\n- **Connections**: Connects to `lyricsController.js` and is used by the main Express app.\n\n### `backend/src/routes/recommendationRoutes.js`\n- **Purpose**: Defines API routes for music recommendations, including AI-generated and community-sourced songs.\n- **Key Functions**:\n - `getRecommendations`: Handles POST requests for AI-generated song recommendations.\n - `communitySongs`: Handles GET requests for community-shared songs.\n- **Connections**: Connects to `recommendationsController.js` and is used by the main Express app.\n\n### `backend/src/routes/historyRoutes.js`\n- **Purpose**: Defines API routes for managing user search history.\n- **Key Functions**:\n - Handles GET and POST requests for user history.\n- **Connections**: Connects to `authMiddleware.js` and `historyController.js`.\n\n### `backend/src/routes/authRoutes.js`\n- **Purpose**: Defines API routes for user authentication.\n- **Key Functions**:\n - Handles POST requests for user registration and login.\n - Handles GET requests for user data.\n- **Connections**: Connects to `authController.js` and is used by the main Express app.\n\n### `backend/src/services/lyricsService.js`\n- **Purpose**: Provides lyrics search functionality by integrating with external APIs (Lyrics.ovh and Genius).\n- **Key Functions**:\n - Fetches lyrics for a given song title.\n- **Connections**: Used by `lyricsController.js`.\n\n## Other Files\n### `vercel.json`\n- **Purpose**: Configuration file for Vercel deployment, specifying how to build and route the application.\n- **Connections**: Used by Vercel to deploy the backend server.\n\n### `.gitignore`\n- **Purpose**: Specifies files and directories to ignore for Git version control.\n\n### `install.sh`\n- **Purpose**: Empty file with no executable code.", "data_flow": "# Data Flow\n\n## Rendering/Execution Flow\n\n1. **Application Startup**:\n - The React application initializes with `AuthContext.jsx` providing authentication state to the entire app via `AuthProvider`.\n - The main routing structure determines which components render based on the URL path.\n\n2. **Authentication Flow**:\n - User lands on the login/registration page (`Auth.jsx`), which toggles between `Login` and `Register` components.\n - When the user submits the login form (`Login.jsx`), it calls the `login` function from `AuthContext.jsx`.\n - `AuthContext.jsx` makes API calls to `/api/auth/login` (backend route from `authRoutes.js`).\n - On successful login, `AuthContext.jsx` updates the auth state and redirects to the recommendations page.\n\n3. **Lyrics Display Flow**:\n - When a song is displayed (likely in `Recommendations.jsx` or `Community.jsx`), `LastFmImage.jsx` is used to fetch album/artist images.\n - `LastFmImage.jsx` makes API calls to the Last.fm API using artist and track names.\n - If the image fetch fails, it falls back to a default image.\n\n4. **Recommendations Flow**:\n - User requests recommendations (likely through a form in `Recommendations.jsx`).\n - This triggers an API call to `/api/recommendations` (from `recommendationRoutes.js`).\n - The backend processes the request and returns recommendations.\n\n## Component/Module Relationships\n\n1. **Authentication Flow**:\n - `Auth.jsx` → `Login.jsx`/`Register.jsx` (toggles between forms)\n - `Login.jsx` → `AuthContext.jsx` (calls login function)\n - `AuthContext.jsx` → Backend API (via axios)\n\n2. **Image Display Flow**:\n - `Community.jsx`/`Recommendations.jsx` → `LastFmImage.jsx` (uses component to display images)\n - `LastFmImage.jsx` → Last.fm API (external service)\n\n3. **Lyrics Flow**:\n - Frontend component → `lyricsRoutes.js` → `lyricsController.js` → `lyricsService.js` → External APIs (Lyrics.ovh/Genius)\n\n## State Management\n\n1. **Authentication State**:\n - Managed by `AuthContext.jsx` using React's Context API.\n - State includes `isAuthenticated`, `user`, and `loading` states.\n - Updated via `login`, `register`, and `logout` functions.\n\n2. **Form State**:\n - Local state in `Login.jsx` and `Register.jsx` components.\n - Updated via `handleChange` on input changes.\n - Submitted via `handleSubmit`.\n\n3. **Image State**:\n - Local state in `LastFmImage.jsx` for storing image URLs.\n - Updated when a new artist/track is provided.\n\n## File Dependencies\n\n1. **Frontend Dependencies**:\n - `Auth.jsx` depends on: `Login.jsx`, `Register.jsx`, `Auth.css`\n - `Login.jsx` depends on: `Auth.css`\n - `LastFmImage.jsx` depends on: React hooks\n - `AuthContext.jsx` depends on: React hooks\n\n2. **Backend Dependencies**:\n - `lyricsRoutes.js` depends on: `lyricsController.js`\n - `recommendationRoutes.js` depends on: `recommendationsController.js`\n - `historyRoutes.js` depends on: `authMiddleware.js`, `historyController.js`\n - `authRoutes.js` depends on: `authController.js`", "api_endpoints": "# API Endpoints\n\n## Authentication Endpoints\n\n### `/api/auth/register`\n- **Method**: POST\n- **Description**: Handles user registration.\n- **Request Body**:\n ```json\n {\n \"email\": \"string\",\n \"password\": \"string\",\n \"username\": \"string\"\n }\n ```\n- **Response**:\n ```json\n {\n \"user\": {\n \"id\": \"string\",\n \"email\": \"string\",\n \"username\": \"string\"\n },\n \"token\": \"string\"\n }\n ```\n\n### `/api/auth/login`\n- **Method**: POST\n- **Description**: Handles user login.\n- **Request Body**:\n ```json\n {\n \"email\": \"string\",\n \"password\": \"string\"\n }\n ```\n- **Response**:\n ```json\n {\n \"user\": {\n \"id\": \"string\",\n \"email\": \"string\",\n \"username\": \"string\"\n },\n \"token\": \"string\"\n }\n ```\n\n### `/api/auth/user`\n- **Method**: GET\n- **Description**: Retrieves user data.\n- **Headers**:\n ```\n Authorization: Bearer <token>\n ```\n- **Response**:\n ```json\n {\n \"id\": \"string\",\n \"email\": \"string\",\n \"username\": \"string\"\n }\n ```\n\n## Lyrics Endpoints\n\n### `/api/lyrics`\n- **Method**: GET\n- **Description**: Fetches song lyrics by title.\n- **Query Parameters**:\n ```\n title: string (required)\n ```\n- **Response**:\n ```json\n {\n \"lyrics\": \"string\"\n }\n ```\n\n## Recommendation Endpoints\n\n### `/api/recommendations`\n- **Method**: POST\n- **Description**: Generates AI-based music recommendations.\n- **Request Body**:\n ```json\n {\n \"mood\": \"string\",\n \"genre\": \"string\"\n }\n ```\n- **Response**:\n ```json\n {\n \"recommendations\": [\n {\n \"title\": \"string\",\n \"artist\": \"string\",\n \"album\": \"string\",\n \"image\": \"string\"\n }\n ]\n }\n ```\n\n### `/api/community-songs`\n- **Method**: GET\n- **Description**: Retrieves community-shared songs.\n- **Response**:\n ```json\n {\n \"songs\": [\n {\n \"title\": \"string\",\n \"artist\": \"string\",\n \"album\": \"string\",\n \"image\": \"string\",\n \"sharedBy\": \"string\"\n }\n ]\n }\n ```\n\n## History Endpoints\n\n### `/api/history`\n- **Method**: GET\n- **Description**: Retrieves user search history.\n- **Headers**:\n ```\n Authorization: Bearer <token>\n ```\n- **Response**:\n ```json\n {\n \"history\": [\n {\n \"query\": \"string\",\n \"timestamp\": \"string\"\n }\n ]\n }\n ```\n\n### `/api/history`\n- **Method**: are not valid JSON. Received JSONDecodeError Unterminated string starting at: line 1 column 10009 (char 10008) For troubleshooting, visit: https://docs.langchain.com/oss/python/langchain/errors/OUTPUT_PARSING_FAILURE For troubleshooting, visit: https://docs.langchain.com/oss/python/langchain/errors/OUTPUT_PARSING_FAILURE